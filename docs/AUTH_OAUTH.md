# OAuth2 (short pointer)

LDAP and OAuth2 configuration, discovery API, and UI behaviour are documented in:

**[AUTH_LDAP_AND_OAUTH.md](./AUTH_LDAP_AND_OAUTH.md)**

That file is the canonical reference for **`coordinator.auth`** (internal / DuckDB users), `coordinator.ldap`, `coordinator.oauth2_provider`, and `/api/v4/auth/*` endpoints.
