EXTRA_DIST = \
	sql \
	homer.spec

# NOTE: not sure why some sql is in webhomer/sql and some are in sql
# and some are copied in both places. --Douglas
sqldir = $(WEBROOT)/webhomer/sql
sql_DATA = \
	sql/create_sipcapture.sql

SUBDIRS = \
	captagent \
	scripts \
	webhomer
